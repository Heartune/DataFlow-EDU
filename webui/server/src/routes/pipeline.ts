import { Router } from 'express';

// Placeholder for future pipeline execution, logs, etc.
export function pipelineRoutes(): Router {
  const router = Router();

  // router.post('/pipeline/run', ...);
  // router.get('/pipeline/logs', ...);

  return router;
}
