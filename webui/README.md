# DataFlow-EDU WebUI

Vue 3 + Vite 前端 + Node.js 后端的 Pipeline 看板。

## 启动（本地开发）

```bash
cd webui
npm install
npm run dev
```

- 前端：http://localhost:5173
- 后端 API：http://localhost:3000

## 构建

```bash
npm run build      # 构建前端
npm run build:server  # 构建后端 (可选)
```
